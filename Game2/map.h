#include <iostream>
#include <stdlib.h>

int creatingMapArray (int height, int width); // Creates the map's arary;
int creatingMapPlaces (int height, int width); // Creates the map's places;

#ifndef MAP_H
#define MAP_H

#endif
